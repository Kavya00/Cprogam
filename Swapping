#include<stdio.h>
int main()
{

int a,b,temp;
printf("Enter value of a and b\n");
scanf("%d%d",&a,&b);
temp=a;
a=b;
b=temp;
printf("Before swapping a=%d,b=%d\n",b,a);
printf("After swapping a=%d,b=%d\n",a,b);
 return 0;
}
